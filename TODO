-------------------------------------------------------------------------------

region rendering

-------------------------------------------------------------------------------

allow pausing physics & scene simulation from the editor

-------------------------------------------------------------------------------

rework the current bounding box system

------------------------------------------------------------------------------

Sound stream resource implementation

-------------------------------------------------------------------------------

fix renderer clear color

-------------------------------------------------------------------------------
remote $ROOT as the beggining of the resource tree
just use linux-like convention (start with /)

-------------------------------------------------------------------------------

Appending .wts files to existing assets.
Have one shared .wts file for all demos and individual .wts-s for every demo
All demos load their own and the shared file

Maybe add a group loading (load a specific group only from a file)

-------------------------------------------------------------------------------

orthographics shadow mapping (fix orthographic projection)

-------------------------------------------------------------------------------

migrate demo framework to AEngineFramework

-------------------------------------------------------------------------------
fix up the demo project
pack every asset in a single .zip file (have a shared directory and individual demo directories) but only have one assets file
but multiple scene files (one per demo)

-------------------------------------------------------------------------------

fix the hardcoded stuff like:
	home directory for the editor
	brush assets for the terrain editor
	default godray texture
	etc..
-------------------------------------------------------------------------------

implement a basic blending algorithm, just render all the opaque objects first and then the translucent ones
this will have its flaws but it's good enough for this

-------------------------------------------------------------------------------

pack all the dependency libraries (like the modified LuaPlus) in one directory

-------------------------------------------------------------------------------

create a demo that demonstrates the engines scripting capabilities

-------------------------------------------------------------------------------

maybe do some additional work on the GUI framework (e.g. make multiple windows available,
and layouts)

-------------------------------------------------------------------------------

fix the render target (right now its fairly broken)

-------------------------------------------------------------------------------

move the godray rendering technique to a seperate renderer

-------------------------------------------------------------------------------

delete legacy rendering functions from the renderer

-------------------------------------------------------------------------------

create more demos

-------------------------------------------------------------------------------

create subdirectories for the files (right now everythign is in one directory )

------------------------------------------------------------------------------

setup a doxygen project

------------------------------------------------------------------------------

possibly make all this run on linux which shoulnd't be too hard from the Wt's standpoint

------------------------------------------------------------------------------